<div class="d-flex justify-content-between align-items-start">
  <h3><%= I18n.t('administration.users.headlines.overview') %> (<%= @users.count %>)</h3>
  <%= link_to I18n.t('buttons.add'), [:new, :user, :registration], class: 'btn btn-primary' %>
</div>
<%= render @users %>
